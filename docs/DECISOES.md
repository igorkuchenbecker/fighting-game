# Decisões de arquitetura e projeto

| Data | Decisão | Motivo | Alternativas descartadas |
|---|---|---|---|
| 2026-08-22 | Projeto criado em `~/fighting-game/` | `~` (HOME) não estava vazio | usar `~` diretamente |
| 2026-08-22 | raylib obtido via `FetchContent` (GitHub, tag `5.5`) | stack exige raylib sem editor de engine; build reprodutível sem depender de pacote do sistema | pacote `raylib` do repositório Arch (não estava instalado); git submodule |
| 2026-08-22 | Flags de warning/sanitizer aplicadas via `CMAKE_CXX_FLAGS` nos presets, não em `target_compile_options` | `CMAKE_CXX_FLAGS` só afeta compilação C++ (nosso código); raylib é C puro, compilado com `CMAKE_C_FLAGS` — isso isola nosso `-Werror` sem quebrar o build da lib externa | flags globais via `add_compile_options` no CMakeLists (afetaria também qualquer alvo C++ de terceiros futuro) |
| 2026-08-22 | Loop de timestep fixo com acumulador (`GetFrameTime()` + `while (accumulator >= kFixedDt)`) já na F0, mesmo sem simulação real ainda | estabelece o padrão arquitetural (sim em passos discretos, render interpola/desenha último estado) exigido pelos requisitos de rollback antes que qualquer lógica de jogo exista, evitando retrabalho | `SetTargetFPS(60)` sozinho (acopla sim ao render, proibido pelos requisitos) |
| 2026-08-22 | `BUILD_EXAMPLES`/`BUILD_GAMES` do raylib forçados OFF, `BUILD_SHARED_LIBS` OFF | reduz tempo de build do FetchContent e gera lib estática (sem gerenciar `.so` no runtime) | manter defaults do raylib (build mais lento, exemplos desnecessários) |
| 2026-08-22 | Suprimir leaks de `libnvidia-glcore.so`/`libnvidia-glsi.so` via `__lsan_default_suppressions()` (só compilado quando `__SANITIZE_ADDRESS__` está definido) | driver proprietário NVIDIA aloca estado interno de contexto GL que nunca é liberado por design; ASan acusava ~6.6KB de "leak" nesse driver a cada execução, mesmo com `CloseWindow()` chamado corretamente — confirmado como falso positivo (stack trace 100% dentro de `libnvidia-*.so`, nenhum frame do nosso código ou do raylib) | `ASAN_OPTIONS=detect_leaks=0` (mascararia leaks reais também); ignorar o warning manualmente a cada rodada (não é automatizável/verificável em CI) |
| 2026-08-22 | `InputFrame`/`Direction8` (8 vias + bitmask de botões) introduzidos já na F1, não só na F2 | requisito duro de arquitetura ("input como dado puro", sim nunca lê teclado) é descrito como inegociável desde o início; só o *buffer circular* de 10 frames (item separado no roadmap) fica para a F2 | ler `IsKeyDown` direto dentro de `StepFighter` (violaria o requisito duro) |
| 2026-08-22 | `Direction8` usa `Left`/`Right` absolutos, não `Forward`/`Back` relativos a um oponente | ainda não existe P2 nem conceito de "de costas" (chega na F4); semântica relativa antes disso seria enganosa | codificar `Forward`/`Back` desde já com um "facing" fixo provisório |
| 2026-08-22 | Input amostrado 1x por iteração do loop externo (fora do `while` de steps fixos) e reaproveitado se mais de um step de simulação rodar no mesmo frame de render | requisito duro proíbe leitura de input *dentro* do step de simulação; amostrar por tick exigiria o buffer/timing que só chega na F2 | amostrar dentro do step (violaria o requisito); um `InputFrame` por tick simulado (adiado para a F2 junto do buffer) |
| 2026-08-22 | Câmera fixa implementada implicitamente (coordenadas de mundo = coordenadas de tela, sem `Camera2D` explícito) | nada ainda dá scroll/zoom; um objeto `Camera2D` parado seria só cerimônia sem efeito observável | criar `Camera2D` já agora "para o futuro" (YAGNI) |
| 2026-08-22 | Direção sempre controla velocidade horizontal, mesmo no ar (sem restrição de "controle aéreo") | mantém a F1 simples; pulo para frente/trás já funciona (segura direção ao pular) | modelar aceleração/inércia aérea separada da terrestre (prematuro antes de haver golpes aéreos na F5) |
| 2026-08-22 | `printf("tick")` da F0 removido | seu único propósito era provar que o loop de timestep fixo rodava antes de haver qualquer estado visível; a partir da F1 o retângulo se movendo já é a prova visual, e o print por tick só gera ruído no stdout | manter o print (poluiria stdout a 60/s durante o jogo) |
| 2026-08-22 | Módulos `input.h/.cpp` e `fighter.h/.cpp` extraídos já na F2, com `main.cpp` ainda pequeno (~500 linhas somadas, bem abaixo de 800) | regra do prompt manda extrair "ao passar de ~800 linhas OU na F2 (o que vier primeiro)" — F2 chegou primeiro | esperar o limite de linhas (contradiria a regra explícita) |
| 2026-08-22 | Borda de subida do botão de ataque detectada via campo `Fighter::attack_button_held` (estado interno da simulação), não lida do `InputBuffer` | `InputFrame` deve continuar sendo só dado bruto (bitmask + direção), conforme o requisito duro; "borda de subida" é estado *derivado*, pertence à simulação (`fighter.cpp`), não ao contrato de input | guardar `attack_just_pressed` como campo extra em `InputFrame` (contaminaria o contrato de input com lógica derivada) |
| 2026-08-22 | `InputBuffer` é realmente alimentado (`Push` a cada frame de render) e consultado (`StepFighter` recebe `AtDelay(0)`), não só declarado e ignorado | evita infra morta; deixa pronta a base para motion-input/replay das fases seguintes sem refatoração | criar o buffer só pra dizer que existe e continuar lendo `InputFrame` avulso |
| 2026-08-22 | FSM completa com os 14 estados do requisito duro (incl. `BlockStanding`, `Hitstun`, `Knockdown`, `Win`/`Lose` etc.) declarada já na F2; estados sem gatilho ainda viram self-loop explícito (`return fighter.state;`) até F3/F4 trazerem oponente/combate real | requisito duro pede "FSM completa" na F2; self-loop documentado não é código morto (executa toda vez que o estado está ali), só não tem porta de entrada ainda | esperar F3/F4 para declarar o enum inteiro (violaria "FSM completa" já na F2) |
| 2026-08-22 | `Fighter::facing_right` fixo em `true` por enquanto; `WalkForward`/`WalkBackward` já usam essa semântica (`Right` = forward) | sem P2 ainda não há "de costas para o quê"; a lógica de virar para encarar o oponente é da F4 | usar `Left`/`Right` cru no lugar de `WalkForward`/`WalkBackward` (não bateria com o nome dos estados exigidos) |
| 2026-08-22 | Ataque neutro não cancelável e indisponível no ar (só dispara a partir de Idle/Walk/Crouch) | moveset real (leve/médio/pesado, antiaéreo, cancelamentos) é escopo da F5; por ora só existe 1 golpe de teste para provar startup/active/recovery | permitir atacar no ar já na F2 (adiantaria escopo do "salto com ataque" da F5) |
| 2026-08-22 | Módulo `combat.h/.cpp` extraído na F3, seguindo a ordem `input, fighter, combat, stage, game` já anunciada | próximo item da ordem de extração definida no prompt | deixar hit/hurtbox dentro de `fighter.cpp` (misturaria FSM/física com resolução de combate) |
| 2026-08-22 | Segundo `Fighter` ("dummy"), parado, sem input real, adicionado só pra existir um alvo pro hit/hurtbox | "acerto gera dano/hitstun/pushback" exige um defensor; P2 jogável (teclado/gamepad) só chega na F4 — sem esse corpo de teste não dava pra validar combate nenhum | esperar a F4 pra ter um segundo lutador (impossível testar combate sem alvo); IA de verdade pro dummy (isso é F7) |
| 2026-08-22 | `kFighterStandHeight`/`kFighterCrouchHeight` promovidas de `main.cpp` pra `fighter.h` | `combat.cpp` agora também precisa (hurtbox muda de altura ao agachar); só render usava antes | duplicar as constantes em `combat.cpp` (violaria "central, sem número mágico espalhado") |
| 2026-08-22 | Bloqueio checado reativamente dentro de `ResolveCombat` (lê o input atual do defensor no instante do hit), sem um estado de "guarda" persistente | `BlockStanding`/`BlockCrouching` continuam self-loop — viram stance visual de verdade só na F4, quando segurar "pra trás" significa algo o jogo inteiro, não só no frame do hit; a mecânica de bloquear/tomar chip/blockstun já funciona sem isso | modelar a stance de guarda já na F3 (exigiria saber a posição do oponente dentro de `ComputeNextState`, que hoje só recebe o próprio `Fighter`) |
| 2026-08-22 | `ApplyHitReaction`/`ApplyBlockReaction` (fighter.cpp) chamadas por `combat.cpp` para aplicar Hitstun/Blockstun, em vez de `combat.cpp` escrever `Fighter::state` direto | mantém a regra da F2 ("único ponto de transição") mesmo com um evento vindo de fora da FSM — combat.cpp só informa o fato, fighter.cpp decide como reagir | deixar `combat.cpp` atribuir `fighter.state = ...` diretamente (quebraria a regra e espalharia a lógica de transição em 2 arquivos) |
| 2026-08-22 | Lista de estados que permitem controle direcional virou allow-list (`Idle/WalkForward/WalkBackward/Jump`) em vez da deny-list da F1/F2 | a deny-list antiga (`!= Crouch && != Attack`) deixaria `Hitstun`/`Blockstun` andando por input, o que é errado; allow-list trava automaticamente qualquer estado novo (Knockdown, Wakeup, Win, Lose) sem precisar lembrar de adicionar exceção depois | manter deny-list e adicionar `Hitstun`/`Blockstun` na exclusão manualmente (funcionaria, mas cada estado novo exigiria lembrar de excluir) |
| 2026-08-22 | Combo counter, knockdown/wakeup real, juggle e medidor de super **não** entraram na F3 | não estão no bullet de F3 do roadmap (que lista só frame data/hit-hurtbox/dano/hitstun/pushback/bloqueio/blockstun/chip); combo counter é elemento de HUD (F4), knockdown/KO fecha o sistema de round (F4), juggle/super acompanham o moveset real (F5) — são requisitos duros gerais, mas o roadmap os distribui em fases diferentes | implementar tudo do requisito duro de uma vez na F3 (inflaria a fatia muito além de "30–90 min", e sem HUD/round pra mostrar combo/KO ainda não faz sentido visual) |
| 2026-08-22 | F4 dividida em sub-fatias (F4a: P2 real; F4b: round/timer/KO; F4c: HUD), cada uma com seu próprio commit | o bullet único da F4 no roadmap (P2+gamepad, round best-of-3, timer, intro, KO, HUD) é grande demais pra uma fatia de "30–90 min"; a regra geral de fatias pequenas e verticais tem prioridade sobre o agrupamento do bullet | um commit gigante cobrindo o bullet inteiro da F4 (violaria "fatias pequenas") |
| 2026-08-22 | Gamepad lido via eixo esquerdo (com deadzone 0.4) OU d-pad, combinado com o teclado por OR (qualquer fonte pressionada vale) | "suporte a gamepad" no roadmap não especifica qual jogador; deixar os dois jogadores usarem teclado OU gamepad (P1=slot 0, P2=slot 1) cobre o requisito sem forçar hardware que o ambiente de dev pode não ter | exigir gamepad conectado (quebraria em máquina sem gamepad); só ler stick sem d-pad (menos compatível) |
| 2026-08-22 | `UpdateFacing(a, b)` chamado 1x por tick, antes dos dois `StepFighter`, define `facing_right` pela posição relativa atual | função pura de 2 posições, determinística; precisa rodar antes de `StepFighter`/`ResolveCombat` pra que `WalkForward`/`WalkBackward` e o lado do hitbox já reflitam a orientação correta no mesmo frame | recalcular facing dentro de `StepFighter` (exigiria acesso ao outro `Fighter`, quebrando a assinatura pura `(Fighter&, InputFrame)`) |
| 2026-08-22 | `BlockStanding`/`BlockCrouching` continuam self-loop mesmo com P2 real e facing dinâmico | bloqueio já funciona via checagem reativa em `ResolveCombat` (`CanBlock` aceita Idle/Walk/Crouch); um estado dedicado de "guarda" seria visualmente idêntico a `WalkBackward`/`Crouch` sem mudar nenhum comportamento — critriaria estado sem função real | criar os estados agora só pra "completar" a FSM visualmente (feature sem comportamento distinto, oposto do que as instruções pedem) |
| 2026-08-22 | Módulos `stage.h/.cpp` (arena/render) e `game.h/.cpp` (round/match) extraídos na F4b | próximos itens da ordem de extração (`input, fighter, combat, stage, game`) — round/match é claramente "game", chão/paredes é claramente "stage" | deixar `Match`/`UpdateMatch` dentro de `main.cpp` (ia crescer demais e main.cpp deixaria de ser só orquestração fina) |
| 2026-08-22 | `kFixedDt` promovido pra `game.h` (de constante local em `main.cpp`); `kScreenWidth`/`kScreenHeight` promovidos pra `stage.h` | `game.cpp` (timer do round) e `stage.cpp` (fundo da arena) agora também precisam desses valores — mesma regra de "central, sem duplicar" já aplicada a outras constantes | duplicar os valores em cada arquivo (arriscaria dessincronia se um mudasse e o outro não) |
| 2026-08-22 | Timer do round decrementado por `kFixedDt` (constante) a cada tick de simulação em `RoundPhase::Fighting`, não por `GetTime()`/relógio de parede | requisito duro proíbe leitura de relógio dentro do step de simulação; decrementar por um valor fixo e constante mantém determinismo (replay/rollback dão o mesmo resultado sempre) | usar `GetFrameTime()` ou `GetTime()` no timer (variável, quebraria determinismo, e leria relógio de dentro do "step") |
| 2026-08-22 | `Knockdown` só é usado quando o round termina por nocaute (`RoundEndReason::Knockout`); round decidido por tempo (`Timeout`) manda o perdedor direto pra `Lose`, sem passar por `Knockdown` | `Knockdown` representa fisicamente "apanhou até cair"; no time-up ninguém apanhou o suficiente pra cair, só perdeu por vida menor — usar Knockdown ali seria visualmente incoerente | usar `Knockdown` pra qualquer perdedor de round, independente do motivo (incoerente com o nome do estado) |
| 2026-08-22 | `Win`/`Lose` são atribuídos ao fim de CADA round (não só ao fim da partida); no `MatchOver` eles simplesmente não são resetados, então a pose do último round vira a pose de "vitória/derrota de partida" de graça | evita um 5º par de estados só pra "vitória de partida" (que seria visualmente idêntico); a maioria dos jogos de luta já reaproveita a pose de round-win como pose de match-win na última rodada | criar `MatchWin`/`MatchLose` distintos de `Win`/`Lose` (estado extra sem diferença visual/comportamental ainda) |
| 2026-08-22 | `Wakeup` segue sem gatilho | só faz sentido com um knockdown "soft" que não termina o round (ex.: launcher de combo que derruba mas deixa continuar) — isso é F5+; aqui todo knockdown termina o round, não tem "acordar" pra quê | inventar um cenário de wakeup artificial só pra popular o estado (feature sem uso real) |
| 2026-08-22 | Caminho de `Timeout` (round decidido por tempo) não foi exercitado ponta-a-ponta no smoke test — só o de `Knockout` | 99s reais de timer levariam ~5940 ticks pra esgotar (impraticável no smoke test); `Timeout` reusa exatamente a mesma `EndRound()` já provada pelo caminho de Knockout, só troca a condição de entrada (`timer<=0` em vez de `health<=0`) e pula o `ApplyKnockdownReaction` | forçar o timer a esgotar de verdade no teste (30x mais lento pra validar um `if` que já reusa código testado) |
| 2026-08-22 | Overlay de round ("ROUND N"/"FIGHT!"/timer numérico/"K.O."/"TIME UP"/resultado da partida) entrou na F4b, não esperou a F4c | esses textos são parte do PRÓPRIO sistema de round (senão a F4b fica muda/invisível, sem como confirmar visualmente que funciona jogando); o bullet de "HUD" da F4 no roadmap lista especificamente barras de vida/medidor/combo/retratos — elementos diferentes, persistentes durante o jogo, não textos de transição de round | esperar a F4c pra desenhar qualquer texto (deixaria a F4b sem nenhuma confirmação visual jogável) |
| 2026-08-22 | Ganhos de medidor de super (`kSuperGainOnHit*`/`kSuperGainOnBlock*`) são constantes genéricas em `combat.cpp`, não campos em `MoveData` | só existe 1 golpe até a F5; criar um campo em `MoveData` pra um valor que hoje é sempre o mesmo seria estrutura sem uso real ainda — quando o moveset da F5 chegar (golpes com ganhos de meter diferentes), aí sim vira campo da tabela | adicionar `super_gain` em `MoveData` já agora (YAGNI — nenhum golpe precisa de um valor diferente do outro porque só existe 1) |
| 2026-08-22 | Escala de dano por combo: 100% no 1º hit, −10% por hit adicional, piso 50% (`ComboDamageScale`) | requisito duro pede "combo counter com escala de dano" explicitamente; valores são placeholder razoável — balanceamento de verdade é trabalho da F5 (que também é quando golpes conseguem de fato encadear, ver decisão abaixo) | não escalar dano nenhum (não cumpriria o requisito duro) |
| 2026-08-22 | `combo_hits` reseta em `StepFighter` (fighter.cpp) ao SAIR de `Hitstun`, não em `ApplyHitReaction` | `ApplyHitReaction` roda toda vez que um hit conecta (inclusive hits que CONTINUAM o combo — resetar ali zeraria o contador a cada hit, nunca contando 2+); resetar só na transição de saída (`Hitstun`→outro estado) é o momento certo: "combo acabou porque o defensor voltou a agir" | resetar dentro de `ApplyHitReaction` (bug: combo nunca passaria de 1) |
| 2026-08-22 | Combo de 2+ hits reais (encadeados em timing de jogo) confirmado como **inalcançável** com o conteúdo atual: recovery(10)+startup(6)=16 frames > hitstun(14) do próprio golpe, e o golpe não é cancelável | com só 1 normal não cancelável, o atacante nunca consegue acertar de novo antes do defensor sair do hitstun; combos de verdade dependem do moveset/cancelamentos da F5 | a lógica de escala/contagem foi validada isoladamente no smoke test, forçando 2 chamadas de `ResolveCombat` no mesmo hitstun (documentado como tal, não como gameplay real) |
| 2026-08-22 | `DrawHud` mora em `game.cpp` (junto com `DrawMatchOverlay`), não em `main.cpp` nem em `fighter.cpp` | é apresentação de dados de partida (vida/meter/combo/retrato), mesma "camada" que já desenha o overlay de round — mantém `main.cpp` fino (só orquestra o loop) e `fighter.cpp` livre de qualquer chamada de render | desenhar o HUD em `main.cpp` (repetiria o padrão que já rejeitamos pro overlay de round) |
| 2026-08-22 | Barra de P2 ancorada na borda direita da tela e esvazia da esquerda pra dentro (espelhada), em vez de crescer/encolher do mesmo lado que a de P1 | convenção visual padrão do gênero (barra de cada jogador "aponta" pro centro da tela, onde a luta acontece) — mais legível que duas barras idênticas indo pro mesmo lado | duas barras iguais ancoradas à esquerda (funcionaria, mas ficaria menos legível/idiomático pro gênero) |
| 2026-08-22 | Retratos placeholder = quadrado sólido com a cor do lutador (`kP1Color`/`kP2Color`) + borda | pedido explícito do roadmap ("retratos placeholder"); arte de verdade é F7 ("spritesheets reais") | pular retrato (o requisito pede explicitamente, mesmo que placeholder) |
| 2026-08-22 | F5 também dividida em sub-fatias (F5a: moveset básico leve/médio/pesado/agachado; salto+ataque, projétil, super e balanceamento real ficam pra depois) | bullet único da F5 no roadmap é grande demais pra uma fatia — mesma regra já aplicada à F4 | um commit gigante cobrindo o bullet inteiro da F5 |
| 2026-08-22 | `MoveId` movido de `combat.h` pra `fighter.h` | `Fighter::current_move` (novo campo) precisa do tipo; `combat.h` já inclui `fighter.h`, então mover pra lá evita dependência circular (fighter.h não pode incluir combat.h) | manter `MoveId` em combat.h e criar um enum "espelho" em fighter.h (duplicaria a fonte de verdade) |
| 2026-08-22 | `Fighter::attack_button_held` (bool) virou `Fighter::buttons_held` (bitmask `uint8_t`), detecção de borda de subida genérica bit a bit (`buttons_down & ~buttons_held`) | agora são 3 botões (leve/médio/pesado); um bool por botão funcionaria mas o bitmask escala pra mais botões futuros (F5+) sem crescer o struct | 3 bools separados (`light_held`, `medium_held`, `heavy_held` — funcionaria, mas não escala tão bem e é mais código repetido) |
| 2026-08-22 | 4 golpes nesta fatia: `LightStanding`/`MediumStanding`/`HeavyStanding`/`CrouchingLight` — não uma matriz completa de 3 forças em pé × 3 agachadas | roadmap pede "leve/médio/pesado, antiaéreo, agachado" (5 conceitos, não 6 combinações); agachado sempre usa o golpe leve independente do botão pressionado, cumprindo "1 golpe agachado" sem inflar a tabela | implementar as 3 forças também agachadas (nada no roadmap pede isso; overengineering) |
| 2026-08-22 | "Antiaéreo" implementado como o `HeavyStanding` ganhando um hitbox alto/alongado verticalmente (offset y −170, altura 110), em vez de um golpe dedicado com input próprio | é assim que vários fighting games reais descrevem normais "que servem de antiaéreo" (ex.: socos fortes parados) sem precisar de um botão/motion input a mais; testado isoladamente (defensor no ar perto do ápice do pulo) e conectou corretamente | criar um `MoveId::AntiAir` separado com input dedicado (ex.: segurar cima+pesado) — adicionaria complexidade de input sem pedido explícito de motion input no roadmap |
| 2026-08-22 | `fighter.cpp` passou a incluir `combat.h` (pra `AdvanceAttackPhase` ler `GetMoveData(fighter.current_move).startup/active/recovery_frames` da tabela central, em vez de constantes fixas) | com múltiplos golpes de duração diferente, o timer de fases não pode mais usar 3 constantes fixas — a duração agora vem da tabela central (mesma tabela que já tem dano/hitbox/etc, sem duplicar) | duplicar startup/active/recovery como constantes em fighter.cpp (violaria "central, sem número mágico espalhado" — a mesma info já mora em combat.h) |
| 2026-08-22 | Mapa de teclas expandido: P1 leve=Espaço, médio=Enter, pesado=Shift-direito; P2 leve=Ctrl-esquerdo, médio=Shift-esquerdo, pesado=Q; gamepad usa os 3 botões de face inferior/direito/esquerdo (A/B/X num padrão Xbox) | precisa de 3 botões de ataque por jogador agora; escolhido por proximidade física das teclas de movimento de cada jogador (não é config rebindável — isso é trabalho de UI fora do escopo do roadmap) | pedir ao usuário pra escolher o mapa de teclas (violaria "zero perguntas avulsas" — decisão tomada e documentada aqui) |
| 2026-08-22 | `JumpingLight` ignora a força do botão (leve/médio/pesado) — sempre o mesmo golpe aéreo, só verifica `attack_just_pressed` | mesma lógica já aplicada ao golpe agachado na F5a (1 golpe por contexto, não uma matriz força×contexto); "salto com ataque" no roadmap não pede 3 variantes aéreas | dar ao pulo as 3 forças também (infla a tabela sem pedido explícito) |
| 2026-08-22 | Atacar no ar ainda zera `velocity.x` (mesma regra de `AllowsDirectionalMovement` que já existia pra `Attack` no chão, sem caso especial pro ar) | consistência: "todo Attack trava movimento horizontal" é uma regra única e simples; um golpe aéreo "mantém o momentum do pulo" seria uma exceção a mais sem pedido explícito no roadmap | dar tratamento especial de momentum aéreo durante o ataque (feature não pedida, mais um caso especial pra manter) |
| 2026-08-22 | Pousar durante a Recovery de um ataque (aéreo ou não) não interrompe o golpe — `AdvanceAttackPhase` roda igual, `is_grounded` vira `true` por fora, sem interação especial | comportamento natural que já emergia da arquitetura (steps independentes: transição de estado, física, avanço de fase — nenhum lê o outro além do necessário); é normal em vários fighting games golpes aéreos "aterrissarem" ainda em recovery | adicionar lógica pra cortar a recovery ao pousar (feature não pedida — "instant overhead" ao pousar é design decision que ninguém pediu) |
| 2026-08-22 | Módulo `projectile.h/.cpp` extraído — `Projectile` é entidade independente do `Fighter` (posição/velocidade/tempo de vida próprios), não um golpe corpo-a-corpo | um projétil precisa continuar existindo/viajando por vários ticks depois que o lutador que o lançou já voltou pro chão/recovery — não cabe no modelo de hitbox-relativa-à-posição-do-atacante que os outros golpes usam | modelar o projétil como só mais uma linha na tabela de hitbox relativa (não funcionaria: hitbox relativa ao atacante impediria o projétil de "se soltar" e continuar sozinho) |
| 2026-08-22 | No máx. 1 projétil ativo por jogador (2 slots fixos, `Match::p1_projectile`/`p2_projectile`), sem lista/vetor de projéteis | atende "1 projétil" do roadmap sem inventar limite de "múltiplos projéteis simultâneos" que ninguém pediu; slots fixos evitam alocação dinâmica em runtime (`std::vector` cresceria/realocaria no meio do jogo) | `std::vector<Projectile>` (permitiria múltiplos, não pedido; e aloca no heap durante o jogo) |
| 2026-08-22 | `CharacterId` (`Warrior`/`Gunner`) com kit **quase idêntico** — a única diferença mecânica é o Gunner trocar o agachado padrão por um projétil (segurando pesado agachado); os outros 4 golpes (leve/médio/pesado/aéreo) são compartilhados, mesma `kMoveTable` pros dois | "2 personagens distintos" no roadmap lista só 1 diferenciador mecânico explícito por personagem ("1 projétil, personagem 2"); frame data plenamente diferente por personagem é trabalho de balanceamento (bullet próprio do roadmap, F5e) — inventar um segundo moveset inteiro agora seria adiantar escopo que tem fatia própria | dar ao Warrior um golpe exclusivo também "pra ficar simétrico" (não pedido; infla escopo sem necessidade) |
| 2026-08-22 | `ResetFighterForNewRound` agora preserva `Fighter::character` (captura antes do `fighter = Fighter{}`, restaura depois) | sem isso, `p2.character = Gunner` setado uma vez em `main.cpp` seria apagado no reset de cada round, voltando pro padrão `Warrior` — Gunner perderia o projétil a partir do round 2 | fazer `ResetFighterForNewRound` não resetar tudo (perderia a garantia de "estado limpo" que o reset dá pros outros campos) |
| 2026-08-22 | `ApplyMoveOutcome` (combat.cpp) extraído como helper compartilhado entre `ResolveCombat` (corpo-a-corpo) e `ResolveProjectileHit` — dano/combo/reação/medidor idênticos, só o pushback difere (projétil só empurra o alvo, sem corpo próprio pra recuar) | evita duplicar ~15 linhas de lógica de combo-scale/reação/medidor entre os dois resolvers | duplicar a lógica em `ResolveProjectileHit` (a mesma regra de combo/escala de dano precisaria ser mantida em 2 lugares) |
| 2026-08-22 | `ResolveCombat` agora retorna cedo se `attacker.current_move == MoveId::Projectile` | um golpe `Projectile` não tem hit/hurtbox corpo-a-corpo de verdade (o `Rectangle` da tabela é `{0,0,0,0}`, placeholder) — quem resolve é `ResolveProjectileHit`, contra o `Projectile` spawnado, não contra o corpo do atacante | deixar `ResolveCombat` tentar resolver Projectile também (acertaria com um hitbox vazio, ou pior, um hitbox não-vazio por engano — redundante e propenso a bug) |
| 2026-08-22 | Spawn do projétil detectado em `game.cpp` (`MaybeSpawnProjectile`, comparando `attack_phase` antes/depois do `StepFighter` daquele tick), não dentro de `fighter.cpp` | mantém `fighter.cpp` sem precisar conhecer `Projectile`/`projectile.h` — a FSM continua "burra" sobre o que existe fora dela; `game.cpp` já é quem orquestra os efeitos colaterais de round (mesmo padrão do KO/Win/Lose) | fazer `StepFighter` spawnar o projétil direto (acoplaria fighter.cpp a projectile.h, quebrando a separação de camadas que as decisões da F3/F4 já estabeleceram) |
| 2026-08-22 | Segurar baixo+pesado no MESMO tick, partindo de Idle, dispara o golpe **em pé** (não o agachado/projétil) — descoberto no smoke test, confirmado como comportamento correto, não bug | `ComputeNextState` checa `attack_just_pressed` antes de `wants_down`; no primeiro tick o lutador ainda está em `Idle` (não teve a chance de virar `Crouch` antes do ataque dentro do mesmo tick) — bate com a maioria dos fighting games reais (precisa já estar agachado, não só apertar os dois ao mesmo tempo) | reordenar os checks pra dar prioridade a `wants_down` sobre o ataque (mudaria o feel de "cima cancela pulo" etc. em outros casos, sem necessidade) |
| 2026-08-22 | Gatilho do super: segurar médio+pesado juntos (`wants_super`), sem exigir os dois no mesmo tick exato — só os dois bits DOWN agora, com pelo menos 1 borda de subida recente (`any_attack_just_pressed`) | não existe sistema de motion input (quarter-circle etc.) implementado; "2 botões juntos" é o gatilho mais simples possível pro requisito duro "ativa super"; exigir sincronia de frame-perfect nos dois seria hostil ao jogador sem necessidade | implementar motion input de verdade (quarter-circle+botão) — trabalho considerável (ler histórico do `InputBuffer`, reconhecer padrão de direção) sem pedido explícito de "motion input" no roadmap |
| 2026-08-22 | Super tem prioridade máxima em `DetermineMove` — checado antes de `Jump`/`Crouch`/força do botão; médio+pesado com medidor cheio sempre vira Super, não importa o estado antes do ataque | reversals de super costumam funcionar "de qualquer lugar" nos fighting games reais (você não quer estar preso num golpe fraco quando tentou dar super); manter uma regra simples (super sempre vence) evita casos especiais por estado | checar super só a partir de Idle/Walk (mais restritivo, sem necessidade clara — nada no roadmap pede essa restrição) |
| 2026-08-22 | Super consome o medidor inteiro (100→0) ao ativar, sem meio-termo/parcial | requisito duro diz só "ativa super com invulnerabilidade inicial", não menciona custo parcial/gradual; consumir tudo é a interpretação mais simples e o padrão mais comum do gênero (1 barra = 1 super) | custo parcial (ex.: 50 do medidor) — nenhuma indicação no roadmap de que o super deveria ser "mais barato" |
| 2026-08-22 | Invulnerabilidade só durante `AttackPhase::Startup` do super (`IsInvulnerable`) — não durante Active/Recovery | requisito duro pede especificamente "invulnerabilidade **inicial**"; é o padrão "reversal super" do gênero: você é intocável enquanto o golpe carrega, mas fica exposto quando ele já saiu (senão o super seria estupidamente seguro pra sempre) | invulnerabilidade durante o golpe inteiro (contradiria "inicial" do requisito, e desbalancearia o super) |
| 2026-08-22 | `IsInvulnerable` exposto publicamente em `fighter.h` (não só uma função local de `combat.cpp`) | usado em 2 lugares com motivos diferentes: `combat.cpp` (guard de hit) e `main.cpp` (feedback visual — retângulo fica branco durante a invuln, pra o jogador ver a janela) | manter só dentro de combat.cpp (main.cpp não teria como dar feedback visual da invulnerabilidade) |
| 2026-08-22 | F5e (balanceamento "de verdade") feito como análise matemática de vantagem de frame no bloqueio (`blockstun_frames − recovery_frames`, convenção padrão do gênero), não por feel de jogo real | sem ferramenta de simulação de input no ambiente, não dá pra "jogar e sentir" — a análise de frame advantage é o proxy mais rigoroso disponível sem um humano jogando; resultado por golpe: Light −2, Medium −4, Heavy −6, CrouchingLight −2, JumpingLight +1 (correto pro gênero: jump-in levemente positivo), Projectile −11 (ok, raramente punível à distância), Super −12 (arriscado no bloqueio, compensado pela invuln no startup) — todos dentro do esperado, nada quebrado | fingir que houve playtest real (enganoso); deixar a fatia sem nenhuma verificação (não cumpriria "balanceado de verdade" nem minimamente) |
| 2026-08-22 | `kProjectileLifetimeFrames` 90→130 | a análise revelou que o projétil expirava (90 frames × 10px/frame = 900px) antes de cruzar a arena inteira (~1080px, kArenaRight−kArenaLeft) — um "zoner" sem alcance de tela cheia não cumpre a função de um projétil de verdade; 130×10=1300px cobre com folga, testado via `StepProjectile` em loop até sair da arena (chegou ativo, 105 ticks) | aumentar a velocidade em vez do tempo de vida (mudaria o timing de reação do oponente, um ajuste bem mais sensível ao "feel" sem poder testar) |
| 2026-08-22 | `MoveId::JumpingLight` teve `active_frames` reduzido de 6 pra 4 | a análise comparativa mostrou que o golpe aéreo tinha active/recovery iguais ou melhores que `LightStanding` E mais dano (10 vs 8) — dominava o golpe terrestre em toda categoria, sem nenhum trade-off que compensasse o risco de já estar comprometido no ar; com active=4 (igual ao terrestre), o dano maior fica sendo a única vantagem, compensada pelo risco do pulo | reduzir o dano do golpe aéreo em vez do active window (a mecânica de "jump-in forte" é intencional no gênero; o problema real era a JANELA de acerto generosa demais, não o dano) |
| 2026-08-22 | `--selftest` roda headless (nunca chama `InitWindow`) — detectado por `strcmp(argv[i], "--selftest")` antes de qualquer coisa gráfica em `main()` | a prova de determinismo é só sobre a simulação (`UpdateMatch`), não precisa de janela/GPU; headless roda mais rápido (sem overhead de render/vsync) e funciona em CI sem display — bateu nos dois presets (release E debug/ASan), inclusive com o MESMO hash entre os dois builds (bônus de confiança: nenhuma UB sendo explorada diferente por nível de otimização) | abrir a janela mesmo pro selftest (mais lento, quebraria em CI sem X11/Wayland, e não teria motivo — a simulação não desenha nada) |
| 2026-08-22 | Hash do estado final calculado campo a campo (`HashFighter`/`HashMatch`/`HashProjectile`, cada campo hasheado individualmente), não via `memcpy`/hash do struct inteiro | bytes de padding entre campos de um struct C++ não são garantidos inicializados; hashear a memória crua correria o risco de pegar lixo de pilha que varia entre execuções por motivos alheios à lógica do jogo, gerando falso-positivo de "não determinístico" | hash do struct inteiro via `sizeof` (mais simples de escrever, mas arriscado — o próprio objetivo do teste é provar determinismo, não pode ter ruído próprio) |
| 2026-08-22 | Inputs do `--selftest` gerados por um LCG (`state = state*1664525+1013904223`) com seed fixa (12345 pro P1, 67890 pro P2), não `std::rand()`/`<random>` | determinístico por construção (mesma seed sempre gera a mesma sequência, garantido pela matemática do LCG, sem depender de estado global tipo `srand()`); simples de implementar sem puxar `<random>` só pra isso | `std::mt19937` com seed fixa (funcionaria igual, mas é mais pesado pra uma necessidade tão simples — só precisa de "variedade determinística", não qualidade estatística de RNG) |
| 2026-08-22 | Módulo `selftest.h/.cpp` extraído (não inline em `main.cpp`) | ~130 linhas de lógica de hash/LCG/harness não são "orquestração fina" — mantém `main.cpp` no papel de só ler argumentos e delegar | inline em `main.cpp` (infla o arquivo que devia ficar fino, mistura 2 responsabilidades bem diferentes: rodar o jogo interativo vs. rodar uma prova de determinismo) |
| 2026-08-22 | `DrawFighter`/`FighterColor`/`FighterDrawHeight` movidos de `main.cpp` (namespace anônimo) pra `game.cpp` (só `DrawFighter` exposta em `game.h`) | o modo treino (F6b) também precisa desenhar lutadores; estavam presos ao namespace anônimo de `main.cpp` (linkage interno, inacessível de outro `.cpp`) | duplicar `DrawFighter` em `training.cpp` (2 fontes de verdade pro mesmo desenho, uma delas ia dessincronizar cedo ou tarde) |
| 2026-08-22 | Modo treino ativado por flag `--training` (não menu/tecla em runtime) | mesma lógica do `--selftest`: não existe sistema de menu (fora do escopo do roadmap), e flag de linha de comando é o jeito mais simples de selecionar um modo alternativo sem construir UI nova | criar uma tela de seleção de modo (partida normal vs. treino) — sistema de menu não pedido em lugar nenhum do roadmap |
| 2026-08-22 | Dummy "infinito" = vida sempre volta a 100 assim que chega a 0 (`if (dummy.health <= 0) ResetFighterForNewRound(...)`), sem passar por round/KO/Match nenhum | interpretação mais simples de "dummy infinito": o objetivo do modo treino é nunca parar de bater, não simular partidas; reusar `Match`/`RoundPhase` pro treino exigiria uma variante inteira do fluxo de round só pra suprimir KO/timer/best-of-3 | reusar `UpdateMatch`/`Match` no treino com round "desligado" (mais complexo: exigiria um `RoundPhase::Training` novo espalhado pela lógica de round inteira, pra suprimir tudo que já existe) |
| 2026-08-22 | Jogador do modo treino é sempre `Warrior` (não `Gunner`) | manter o escopo da F6b restrito ao que o roadmap pede (dummy, hitboxes, frame data) — deixar o jogador praticar o projétil do Gunner em treino é uma melhoria razoável mas não pedida, e exigiria reaproveitar `MaybeSpawnProjectile` (hoje interno a `game.cpp`) | expor `MaybeSpawnProjectile` publicamente e dar Gunner ao jogador do treino (funcionalidade extra não solicitada, adicionaria uma dependência nova só pra isso) |
| 2026-08-22 | Tecla F1 alterna exibição de hit/hurtbox via `IsKeyPressed` (raylib, edge-detectado pela própria API, 1x por frame de render) | é UI/debug de treino, não faz parte do contrato de input da simulação (`InputFrame`/`ReadInputFrame`) nem do "step de simulação" que o requisito duro proíbe de ler input — é exatamente o mesmo tipo de leitura de tecla já usado pra decisões de render/loop externo (ex.: `WindowShouldClose`) | rotear o toggle pelo `InputFrame`/`ButtonBit` (misturaria uma tecla de debug/UI com o contrato de input do jogo, que é sobre o que o PERSONAGEM faz, não sobre debug) |
| 2026-08-22 | Overlay de frame data sempre visível no treino (sem tecla de toggle) — só o hit/hurtbox tem tecla | o texto do roadmap distingue os dois: "exibir hitboxes **com tecla**, overlay de frame data" (frase separada, sem menção de tecla) — overlay de frame data é o propósito central do modo treino, faz sentido ficar sempre ligado | dar toggle também pro overlay de frame data (não pedido, e esconderia a informação mais útil do modo por padrão) |
| 2026-08-22 | F7 dividida em sub-fatias (F7a: sprites; provavelmente F7b áudio, F7c IA dummy) | mesma regra já aplicada às F4/F5/F6 — mesmo sendo a fase "opcional/se sobrar fôlego", cada pedaço ainda merece ser uma fatia verificável e commitada por si | um commit só cobrindo a F7 inteira (contradiria "fatias pequenas", ainda mais numa fase que pode parar a qualquer sub-fatia) |
| 2026-08-22 | Sprite é uma textura estática por personagem (`assets/warrior.png`/`assets/gunner.png`), não uma spritesheet animada de verdade (com frames de idle/walk/attack) | sem arte real disponível no ambiente (não gero pixel art à mão), animação de verdade precisaria de um layout de frames que não existe pra testar — uma textura estática já cumpre "carregue spritesheet SE existir... senão retângulo" no sentido literal (troca a FORMA desenhada, mantém toda a lógica de cor/estado que já existia) | inventar um sistema de sub-retângulos de animação sem nenhum asset real que o exercite (especulativo, não verificável) |
| 2026-08-22 | `DrawFighter` ganhou um 3º parâmetro `const CharacterSprite* sprite = nullptr` (default), não uma sobrecarga nova nem uma função `DrawFighterSprite` separada | mantém 1 função só de desenho de lutador (não duplica a lógica de cor/altura por estado que já existia); default `nullptr` preserva binário-compatível o único outro lugar que já chamava `DrawFighter` sem sprite (`training.cpp`, que continua sem arte — ver decisão abaixo) | criar `DrawFighterSprite` como função irmã (duplicaria `FighterColor`/`FighterDrawHeight` ou exigiria expô-las, sem necessidade) |
| 2026-08-22 | Espelhamento da sprite por `facing_right` via largura NEGATIVA no `Rectangle` de origem do `DrawTexturePro` | truque padrão do raylib pra flip horizontal sem carregar uma 2ª textura nem duplicar a arte; já é assim que o motor recomenda fazer | manter 2 texturas por personagem (uma virada pra cada lado) — dobra o espaço em disco e a complexidade de carregamento sem necessidade |
| 2026-08-22 | Tint da sprite reaproveita exatamente `FighterColor` (a mesma cor que colore o retângulo — laranja/vermelho/violeta por fase de ataque, branco na invuln do super, etc.) | preserva 100% do feedback visual de gameplay que já existia (startup/active/recovery, hitstun, blockstun, invulnerabilidade) mesmo quando a arte real substituir os retângulos — sem isso, trocar pra sprite real "esconderia" informação de jogo que o jogador dependia de ler pela cor | ignorar o tint com arte real (perderia o feedback visual de fase/estado que várias fatias anteriores construíram) |
| 2026-08-22 | Sprites geradas via ImageMagick (`assets/warrior.png`/`gunner.png`, retângulos coloridos com "W"/"G") só pra VERIFICAR o caminho de carregamento (confirmado por screenshot — ver `AGENTS.md`), depois deletadas — `assets/` volta a não existir no repo | são placeholders de teste, não arte de verdade; o roadmap distingue claramente "spritesheets reais" (algo que o usuário forneceria) de qualquer coisa que eu geraria — manter os placeholders commitados confundiria o estado real do projeto ("tem arte" vs. "não tem, mas o código já suporta quando tiver") | manter os placeholders no repo como "arte provisória" (o roadmap não pede isso; e um `.png` de teste no controle de versão é ruído) |
| 2026-08-22 | Modo treino (`training.cpp`) continua sem sprites — chama `DrawFighter(fighter, color)` sem o 3º argumento, sempre retângulo | fora do escopo mínimo da F7a (que é sobre a partida normal); o overlay de hit/hurtbox do treino (F6b) já desenha contornos coloridos por cima do lutador — uma sprite ali competiria visualmente com esse overlay de debug, que é o propósito central do modo | carregar sprites também no treino (expandiria escopo sem necessidade, e prejudicaria a legibilidade do overlay de debug que é o ponto do modo) |
| 2026-08-22 | `LoadCharacterSprite`/`UnloadCharacterSprite` seguem o padrão Load/Unload do próprio raylib (não um wrapper RAII customizado tipo destrutor automático) | `Texture2D` é um handle de recurso de GPU que só pode ser liberado com o contexto GL ainda vivo — um destrutor de C++ chamado em ordem de destruição de escopo não tem garantia de rodar antes de `CloseWindow()`; seguir o padrão que o próprio raylib usa (chamado explicitamente, na ordem certa) é mais seguro que tentar RAII por cima de uma API que não foi desenhada pra isso | envolver `Texture2D` numa classe RAII com destrutor que chama `UnloadTexture` (arriscaria descarregar depois do `CloseWindow()` dependendo da ordem de destruição de variáveis locais, UB) |
| 2026-08-22 | `ResolveCombat`/`ResolveProjectileHit` passaram de `void` pra retornar `CombatOutcome` (None/Hit/Blocked); `UpdateMatch` passou de `void` pra retornar `MatchEvents` (hit/bloqueio/pulo/KO de cada jogador nesse tick) | precisava de um jeito de `main.cpp` saber "toca som de hit ou de bloqueio" sem que a simulação (`combat.cpp`/`game.cpp`) precisasse chamar `PlaySound` ela mesma — isso acoplaria `UpdateMatch` (usado também pelo `--selftest` headless, sem áudio inicializado) a `raylib` audio | fazer `combat.cpp`/`game.cpp` chamarem `PlaySound` diretamente (quebraria o `--selftest` headless — chamar função de áudio sem `InitAudioDevice` é undefined behavior/crash dependendo do backend; e violaria a separação sim/apresentação que várias decisões anteriores já estabeleceram) |
| 2026-08-22 | Evento de pulo detectado em `game.cpp` comparando `FighterState` antes/depois de `StepFighter` (`state_before != Jump && state == Jump`), mesmo padrão já usado pra detectar spawn de projétil (`MaybeSpawnProjectile`) | reaproveita a técnica que já funcionava (comparar estado do tick anterior com o atual) em vez de inventar uma segunda forma de detectar transição de estado | adicionar um campo `bool just_jumped` no `Fighter` só pra isso (mancharia o struct de simulação com um conceito que só interessa pra apresentação/áudio) |
| 2026-08-22 | 4 gatilhos de som escolhidos: hit, bloqueio, pulo, KO (`assets/audio/hit.wav`/`block.wav`/`jump.wav`/`ko.wav`) | roadmap só diz "áudio opcional... silencioso se ausentes", sem especificar quais eventos — esses 4 cobrem os momentos mais informativos de feedback sonoro num fighting game (confirma acerto vs. bloqueio, e os dois momentos de maior impacto: pulo e KO) sem inflar a lista | tentar cobrir todo evento possível (som por golpe/por personagem/etc. — nada pedido, e sem asset real pra popular tanta variedade) |
| 2026-08-22 | Modo treino (`training.cpp`) continua sem áudio — chama `ResolveCombat`/`ResolveProjectileHit` direto, ignora o `CombatOutcome` de volta | mesma decisão já tomada pras sprites (F7a): fora do escopo mínimo da fatia, que é sobre a partida normal; ignorar um valor de retorno em C++ não gera erro nem warning, então a mudança de assinatura não quebrou nada no treino | também tocar som no treino (expandiria escopo sem pedido; e o treino já tem seu próprio conjunto de feedback — overlay de frame data, hit/hurtbox) |
| 2026-08-22 | WAVs placeholder gerados via `sox` (tons senoidais simples, 1 por gatilho) só pra confirmar carregamento+reprodução sem crash, depois deletados — `assets/` volta a não existir | mesmo raciocínio da F7a: são só prova de que o caminho de código funciona, não áudio de verdade; manter arquivos de teste no repo confundiria "tem áudio" com "suporta áudio quando existir" | manter os placeholders commitados (ruído no repo, e o roadmap distingue "áudio opcional" fornecido pelo usuário de qualquer coisa gerada por mim) |
| 2026-08-22 | `InitAudioDevice()`/`CloseAudioDevice()` chamados em `main.cpp`, depois de `InitWindow`/antes de `CloseWindow` (ordem não importa entre si — são subsistemas independentes do raylib — mas o padrão comum dos exemplos do próprio raylib é esse) | consistente com o padrão já estabelecido pra sprites (carrega depois da janela existir, descarrega antes dela fechar); modo treino e `--selftest` não chamam `InitAudioDevice` (não precisam, não tocam som) | inicializar áudio incondicionalmente pra todo modo (custo/complexidade sem uso no treino/selftest) |
| 2026-08-22 | IA dummy básica (F7c) entra como modo **separado** `--training-ai` (`RunTrainingMode(bool use_ai_dummy)`), não substitui o dummy estático do `--training` puro | o dummy parado da F6b tem valor próprio (alvo previsível pra estudar frame data/combos — é literalmente pra isso que ele existe); trocar o comportamento padrão do `--training` pra um dummy que anda/foge/bloqueia regrediria esse caso de uso sem ninguém ter pedido a troca — "IA dummy básica" no roadmap é aditivo, não substitutivo | fazer `--training` sempre usar a IA (quebraria o fluxo de treino puro de frame data que a F6b já entregava e validava) |
| 2026-08-22 | `dummy_ai.cpp` tem seu próprio LCG (`NextLcg`, cópia local, mesma fórmula do `selftest.cpp`), não reaproveita o de lá | o LCG do `selftest.cpp` vive num namespace anônimo (linkage interno, inacessível de outro `.cpp`); duplicar 3 linhas de LCG é mais barato e mais desacoplado do que expor/mover pra um módulo `rng.h` compartilhado só pra 2 usos | criar `src/rng.h` compartilhado pra um LCG de 3 linhas usado em só 2 lugares (over-engineering pra economizar uma duplicação trivial) |
| 2026-08-22 | Reação de bloqueio da IA (`player_attacking_close`) tem prioridade absoluta sobre qualquer decisão "aleatória" em andamento — interrompe andar/atacar/parado imediatamente | uma IA "básica" que continuasse andando pra longe e tomando dano enquanto o golpe do jogador já está ativo pareceria quebrada/burra demais, mesmo pros padrões de "básica"; reagir a ameaça imediata é o mínimo de comportamento reativo esperado | deixar a reação de bloqueio competir com o timer de decisão normal (a IA levaria até 59 frames — quase 1s — pra reagir a um golpe já ativo, tempo demais) |
| 2026-08-22 | Ataque da IA é um pulso de 1 tick (`pending_attack` resetado assim que o botão é enviado uma vez), não segura o botão até a próxima decisão | corresponde exatamente ao "borda de subida" que `StepFighter`/`buttons_held` já esperam pra disparar `attack_just_pressed` — segurar o botão por vários ticks não faria diferença nenhuma (só a borda importa), então soltar imediatamente é mais simples e evita confusão sobre "por que o botão ficou pressionado tanto tempo" | segurar `kButtonLight` durante toda a janela de decisão (~30-59 frames) — sem efeito prático adicional, só complica o raciocínio sobre o código |
| 2026-08-22 | Seed da IA fixa (`kDummyAiSeed = 424242`), sem flag de linha de comando pra escolher outra | roadmap pede "IA... com seed" (determinismo), não pede seed configurável pelo usuário — uma constante nomeada já cumpre o requisito duro (nunca `rand()`/`time()`) sem inflar o parsing de `argv` | adicionar `--training-ai-seed=N` (não pedido; mais uma flag pra manter documentada sem necessidade clara) |
| 2026-08-22 | Verificação da IA: comportamentos "andar/parar/apanhar hit/atacar" confirmados por log real rodando `--training-ai` (variedade observada numa única sessão determinística); a reação de bloqueio foi validada por REVISÃO DE CÓDIGO (mesma convenção geométrica `atacante_à_direita ? Left : Right` já testada ao vivo em F3/F4a/F5c/F5d), não por uma nova observação ao vivo nesta fatia | a lógica de bloqueio da IA é trivialmente equivalente (mesmo padrão, só a fonte da decisão muda de "checar input já dado" pra "calcular que input dar"); forçar uma nova observação ao vivo exigiria orquestrar timing exato de RNG vs. script de ataque, custo desproporcional ao risco (baixíssimo) de a linha estar errada | forçar o cenário exato (ajustar seed/timing até bater bloqueio na janela do smoke test) só por completude — desproporcional ao risco real |
